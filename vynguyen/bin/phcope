#!/bin/sh

find -L . -name "*.[pi][hn][pc]" -xtype f -print | grep -v SCCS > cscope.files
find -L . -name "*.module" -xtype f -print | grep -v SCCS >> cscope.files
find -L . -name "*.install" -xtype f -print | grep -v SCCS >> cscope.files
ctags -L cscope.files --language-force=php

cscope 
